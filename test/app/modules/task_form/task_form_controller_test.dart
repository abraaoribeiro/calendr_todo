import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:calendr_todo/app/modules/task_form/task_form_controller.dart';
import 'package:calendr_todo/app/app_module.dart';

void main() {
  initModule(AppModule());
  // TaskFormController taskform;
  //
  setUp(() {
    //     taskform = AppModule.to.get<TaskFormController>();
  });

  group('TaskFormController Test', () {
    //   test("First Test", () {
    //     expect(taskform, isInstanceOf<TaskFormController>());
    //   });

    //   test("Set Value", () {
    //     expect(taskform.value, equals(0));
    //     taskform.increment();
    //     expect(taskform.value, equals(1));
    //   });
  });
}
