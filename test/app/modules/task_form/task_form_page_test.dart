import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:calendr_todo/app/modules/task_form/task_form_page.dart';

void main() {
  testWidgets('TaskFormPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TaskFormPage(title: 'TaskForm')));
    //  final titleFinder = find.text('TaskForm');
    //  expect(titleFinder, findsOneWidget);
  });
}
